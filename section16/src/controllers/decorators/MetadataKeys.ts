// abstracting this into enums is good because it means there are less chances for error
// i wonder what kind of data can go into an enum
export enum MetadataKeys {
  method = 'method',
  path = 'path',
  middleware = 'middleware'
}